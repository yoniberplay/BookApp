
<div class="row" >
    <div class="col-6 offset-3">
        <div class="card">
            <div class="card-header bg-dark text-light">
                <h4 >Esta seguro que desea eliminar la categoria {{category.name}}?</h4>
            </div>
            <div class="card-body" style="background-color: #C7DBD2">
                 <form class="form-inline" action="/delete-category" method="post">                 

                     <input type="hidden" name="categoryId" value="{{category.Id}}" />

                    <button type="submit" class="btn btn-danger">
                        <i class="fa-solid fa-trash"></i> Si</button>
                    <a href="/category" class="btn btn-success">
                        <i class="fa-solid fa-cancel"></i> Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>